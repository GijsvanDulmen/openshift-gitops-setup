# needed for routes external secrets
# and just to play with
# does cycle all the machines!

# apiVersion: config.openshift.io/v1
# kind: FeatureGate
# metadata:
#   name: cluster
#   annotations:
#     argocd.argoproj.io/sync-options: "Delete=false"
# spec:
#   featureSet: TechPreviewNoUpgrade